# CRUD App (Node + Express + SQLite)

Simple single-file frontend served from an Express backend, backed by SQLite.

Setup

1. Install dependencies:

```bash
npm install
```

2. Start server:

```bash
npm start
```

Open http://localhost:3000 in your browser.
